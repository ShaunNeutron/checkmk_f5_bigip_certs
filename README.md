# checkmk_f5_bigip_certs
Finds all certificates on an F5 BIG-IP and determines expiration date. Default certificates are excluded from monitoring. Warning status starts at 30 days and Critical at 10.
